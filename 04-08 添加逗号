//对于一个较大的整数 N(1<=N<=2,000,000,000)
//比如 980364535，我们常常需要一位一位数这个数字是几位数，但是如果在这 个数字每三位加一个逗号，它会变得更加易于朗读。
//因此，这个数字加上逗号成如下的模样：980,364,535请写一个程序帮她完成这件事情
#pragma once
#pragma warning(disable : 4996)
#include<stdio.h>
int main()
{
	int num = 0;
	int k = 0;//统计存入的位数
	scanf("%d", &num);
	char tmp[13] = { 0 };//最长10位数字，所以最多三个逗号
	int i = 0;
	while (num)
	{
		if (k != 0 && k % 3 == 0)//如果位数不为0 且 位数除以3等于0 
		{
			tmp[i++] = ',';//就在后面一位打印 ， 
		}
		tmp[i++] = num % 10 + '0';//把最后一位数字输入，+‘0’是为了转换成字符
		num /= 10; //把最后一位去掉，方便下一次查找
		k++;//位数加1
	}
	//因为这样打印出来是逆序，所以倒着打印即可得到答案
	for (int j = i - 1; j >= 0; j--)
	{
		printf("%c", tmp[j]);
	}
}
